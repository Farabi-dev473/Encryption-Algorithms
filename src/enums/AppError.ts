enum ApplicationError {
    ONLY_ACCEPT_ARRAY = 'Only array is accepted',
    ONLY_ACCEPTED_STRING = 'Only string is accepted',
    ONLY_STRING_AND_ARRAY_ACCEPTED = 'Only string and array are accepted',
    ONLY_NUMBER_ACCEPTED = 'Only number is accepted'
}

module.exports = ApplicationError